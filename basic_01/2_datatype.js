"use strict " // treat all js code as newer version

// alert("hell") // we are using nodejs, not browser

let name = "hello"  // string
let age = 18        //integer
let loggeIn = false  // boolean
let state = null     //null -> standalone value
                     // undefine => no defined value
                     // symbol => unique


                     // object
console.log(typeof  null);
